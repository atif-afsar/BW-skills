export default function SectionEyebrow({ children }) {
  return (
    <p className="mb-4 text-xs font-bold uppercase tracking-[0.2em] text-brand-purple sm:text-sm">
      — {children}
    </p>
  );
}
